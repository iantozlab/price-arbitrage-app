{
  "name": "price-arbitrage-app",
  "version": "1.0.0",
  "description": "Full-stack price arbitrage application",
  "scripts": {
    "build": "echo 'No build needed for Netlify functions'",
    "dev": "netlify dev"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "netlify-cli": "^12.0.0"
  }
}